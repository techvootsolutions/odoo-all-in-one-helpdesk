<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_service_desk_support_contract_list" model="ir.ui.view">
        <field name="name">service.desk.support.contract.list</field>
        <field name="model">service.desk.support.contract</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="partner_id"/>
                <field name="total_hours"/>
                <field name="used_hours"/>
                <field name="remaining_hours"/>
                <field name="date_end"/>
                <field name="state" widget="badge"/>
                <field name="company_id" groups="base.group_multi_company"/>
            </list>
        </field>
    </record>

    <record id="view_service_desk_support_contract_form" model="ir.ui.view">
        <field name="name">service.desk.support.contract.form</field>
        <field name="model">service.desk.support.contract</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_activate" type="object" string="Activate" invisible="state != 'draft'"/>
                    <button name="action_cancel" type="object" string="Cancel" invisible="state in ('cancelled', 'expired')"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_tickets" type="object" class="oe_stat_button" icon="fa-ticket">
                            <field name="ticket_count" widget="statinfo" string="Tickets"/>
                        </button>
                    </div>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="partner_id"/>
                            <field name="product_id"/>
                            <field name="sale_order_id"/>
                        </group>
                        <group>
                            <field name="total_hours"/>
                            <field name="used_hours"/>
                            <field name="remaining_hours"/>
                            <field name="low_hours_threshold"/>
                            <field name="date_start"/>
                            <field name="date_end"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                        </group>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="action_service_desk_support_contract" model="ir.actions.act_window">
        <field name="name">Support Contracts</field>
        <field name="res_model">service.desk.support.contract</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
